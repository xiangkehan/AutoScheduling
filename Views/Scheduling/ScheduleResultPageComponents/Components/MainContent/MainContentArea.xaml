<UserControl
    x:Class="AutoScheduling3.Views.Scheduling.ScheduleResultPageComponents.Components.MainContent.MainContentArea"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:AutoScheduling3.Views.Scheduling.ScheduleResultPageComponents.Components.MainContent"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                Padding="16">
            <local:MainToolbar DataContext="{x:Bind ViewModel}"/>
        </Border>

        <!-- 筛选搜索栏 -->
        <Border Grid.Row="1"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                Padding="16,0,16,16">
            <local:FilterSearchBar DataContext="{x:Bind ViewModel}"/>
        </Border>

        <!-- 内容区域 -->
        <Border Grid.Row="2"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                Padding="16">
            <Grid>
                <!-- 视图模式切换 -->
                <ContentControl Content="{x:Bind ViewModel, Mode=OneWay}">
                    <ContentControl.ContentTemplateSelector>
                        <local:ViewModeTemplateSelector>
                            <local:ViewModeTemplateSelector.GridViewTemplate>
                                <DataTemplate>
                                    <TextBlock Text="网格视图 - 待实现" />
                                </DataTemplate>
                            </local:ViewModeTemplateSelector.GridViewTemplate>
                            <local:ViewModeTemplateSelector.ListViewTemplate>
                                <DataTemplate>
                                    <local:ListView />
                                </DataTemplate>
                            </local:ViewModeTemplateSelector.ListViewTemplate>
                            <local:ViewModeTemplateSelector.PersonnelViewTemplate>
                                <DataTemplate>
                                    <local:PersonnelView />
                                </DataTemplate>
                            </local:ViewModeTemplateSelector.PersonnelViewTemplate>
                            <local:ViewModeTemplateSelector.PositionViewTemplate>
                                <DataTemplate>
                                    <local:PositionView />
                                </DataTemplate>
                            </local:ViewModeTemplateSelector.PositionViewTemplate>
                        </local:ViewModeTemplateSelector>
                    </ContentControl.ContentTemplateSelector>
                </ContentControl>

                <!-- 加载遮罩 -->
                <Grid Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"
                      Background="{ThemeResource LayerFillColorDefaultBrush}"
                      Opacity="0.9">
                    <StackPanel HorizontalAlignment="Center" 
                                VerticalAlignment="Center" 
                                Spacing="16">
                        <ProgressRing IsActive="True" 
                                      Width="60" 
                                      Height="60"/>
                        <TextBlock Text="加载中..." 
                                   Style="{ThemeResource SubtitleTextBlockStyle}" 
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Microsoft.UI.Xaml/DensityStyles/Compact.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
</UserControl>