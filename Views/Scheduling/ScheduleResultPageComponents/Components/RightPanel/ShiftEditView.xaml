<UserControl
    x:Class="AutoScheduling3.Views.Scheduling.ScheduleResultPageComponents.Components.RightPanel.ShiftEditView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:AutoScheduling3.Converters"
    xmlns:local="using:AutoScheduling3.Views.Scheduling.ScheduleResultPageComponents.Components.RightPanel"
    mc:Ignorable="d"
    Background="Transparent">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InvertedBooleanConverter x:Key="InvertedBooleanConverter"/>
    </UserControl.Resources>

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="编辑班次" 
                   FontSize="20" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,24"/>
        
        <!-- 基本信息 -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Spacing="12" Margin="0,0,24,0">
                <TextBlock Text="日期:" FontSize="14" FontWeight="Medium"/>
                <TextBlock Text="时段:" FontSize="14" FontWeight="Medium"/>
                <TextBlock Text="哨位:" FontSize="14" FontWeight="Medium"/>
            </StackPanel>
            
            <StackPanel Grid.Column="1" Spacing="12">
                <TextBlock x:Name="DateTextBlock" Text="2024-01-01" FontSize="14"/>
                <TextBlock x:Name="TimeSlotTextBlock" Text="08:00-12:00" FontSize="14"/>
                <TextBlock x:Name="PositionTextBlock" Text="哨位A" FontSize="14"/>
            </StackPanel>
        </Grid>
        
        <!-- 人员选择 -->
        <StackPanel Grid.Row="2" Margin="0,0,0,24">
            <TextBlock Text="分配人员:" 
                       FontSize="14" 
                       FontWeight="Medium" 
                       Margin="0,0,0,8"/>
            
            <ComboBox x:Name="PersonnelComboBox" 
                      PlaceholderText="选择人员" 
                      Width="300">
                <ComboBoxItem Content="张三"/>
                <ComboBoxItem Content="李四"/>
                <ComboBoxItem Content="王五"/>
                <ComboBoxItem Content="赵六"/>
                <ComboBoxItem Content="孙七"/>
            </ComboBox>
        </StackPanel>
        
        <!-- 时段选择 -->
        <StackPanel Grid.Row="3" Margin="0,0,0,24">
            <TextBlock Text="时段:" 
                       FontSize="14" 
                       FontWeight="Medium" 
                       Margin="0,0,0,8"/>
            
            <StackPanel Orientation="Horizontal" Spacing="12">
                <ComboBox x:Name="StartTimeComboBox" 
                          PlaceholderText="开始时间" 
                          Width="140">
                    <ComboBoxItem Content="00:00"/>
                    <ComboBoxItem Content="04:00"/>
                    <ComboBoxItem Content="08:00"/>
                    <ComboBoxItem Content="12:00"/>
                    <ComboBoxItem Content="16:00"/>
                    <ComboBoxItem Content="20:00"/>
                </ComboBox>
                
                <TextBlock Text="至" 
                           FontSize="14" 
                           VerticalAlignment="Center"/>
                
                <ComboBox x:Name="EndTimeComboBox" 
                          PlaceholderText="结束时间" 
                          Width="140">
                    <ComboBoxItem Content="04:00"/>
                    <ComboBoxItem Content="08:00"/>
                    <ComboBoxItem Content="12:00"/>
                    <ComboBoxItem Content="16:00"/>
                    <ComboBoxItem Content="20:00"/>
                    <ComboBoxItem Content="24:00"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>
        
        <!-- 备注编辑 -->
        <StackPanel Grid.Row="4" Margin="0,0,0,24">
            <TextBlock Text="备注:" 
                       FontSize="14" 
                       FontWeight="Medium" 
                       Margin="0,0,0,8"/>
            
            <TextBox x:Name="RemarksTextBox" 
                     PlaceholderText="输入备注信息" 
                     AcceptsReturn="True" 
                     TextWrapping="Wrap" 
                     MinHeight="100" 
                     Width="300"/>
        </StackPanel>
        
        <!-- 占位符 -->
        <Grid Grid.Row="5"/>
        
        <!-- 操作按钮 -->
        <StackPanel Grid.Row="6" 
                    Orientation="Horizontal" 
                    Spacing="12" 
                    HorizontalAlignment="Right">
            <Button x:Name="CancelButton" 
                    Content="取消" 
                    Click="CancelButton_Click" 
                    Style="{ThemeResource ButtonStyle}"/>
            <Button x:Name="SaveButton" 
                    Content="保存" 
                    Click="SaveButton_Click" 
                    Style="{ThemeResource AccentButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>