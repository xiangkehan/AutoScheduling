<UserControl
    x:Class="AutoScheduling3.Views.Scheduling.ScheduleResultPageComponents.Components.LeftPanel.StatisticsSummaryCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <StackPanel Spacing="12">
        <!-- 标题 -->
        <TextBlock Text="统计摘要" 
                   Style="{ThemeResource SubtitleTextBlockStyle}"/>

        <!-- 统计指标网格 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 硬约束冲突 -->
            <Border Grid.Row="0" Grid.Column="0"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource SystemFillColorCriticalBrush}"
                    BorderThickness="2"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,8,8"
                    PointerPressed="HardConflictCard_PointerPressed">
                <StackPanel Spacing="4">
                    <TextBlock Text="硬约束冲突" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.ConflictStatistics.HardConflictCount, Mode=OneWay, FallbackValue=0}" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                </StackPanel>
            </Border>

            <!-- 软约束冲突 -->
            <Border Grid.Row="0" Grid.Column="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource SystemFillColorCautionBrush}"
                    BorderThickness="2"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,8,8"
                    PointerPressed="SoftConflictCard_PointerPressed">
                <StackPanel Spacing="4">
                    <TextBlock Text="软约束冲突" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="{ThemeResource SystemFillColorCautionBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.ConflictStatistics.SoftConflictCount, Mode=OneWay, FallbackValue=0}" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="{ThemeResource SystemFillColorCautionBrush}"/>
                </StackPanel>
            </Border>

            <!-- 未分配班次 -->
            <Border Grid.Row="0" Grid.Column="2"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,8"
                    PointerPressed="UnassignedCard_PointerPressed">
                <StackPanel Spacing="4">
                    <TextBlock Text="未分配班次" 
                               FontSize="12" 
                               FontWeight="SemiBold" 
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="{x:Bind ViewModel.ConflictStatistics.UnassignedSlotCount, Mode=OneWay, FallbackValue=0}" 
                               FontSize="24" 
                               FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>