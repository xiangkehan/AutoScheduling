<Page
    x:Class="AutoScheduling3.Views.Scheduling.ScheduleGridFullScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:AutoScheduling3.Controls"
    xmlns:dto="using:AutoScheduling3.DTOs"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Grid Grid.Row="0" 
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
              BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
              BorderThickness="0,0,0,1" 
              Padding="20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 标题 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                <FontIcon Glyph="&#xE740;" 
                          FontSize="24" 
                          VerticalAlignment="Center"
                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                <TextBlock x:Name="TitleTextBlock" 
                           Text="全屏视图" 
                           Style="{ThemeResource TitleTextBlockStyle}" 
                           VerticalAlignment="Center"/>
            </StackPanel>

            <!-- 操作按钮 -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <!-- 关闭按钮 -->
                <Button x:Name="CloseButton"
                        Click="CloseButton_Click"
                        Style="{ThemeResource AccentButtonStyle}"
                        AutomationProperties.Name="关闭全屏"
                        AutomationProperties.HelpText="按 Esc 键也可以退出全屏">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE711;" FontSize="16"/>
                        <TextBlock Text="关闭全屏" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Escape" />
                    </Button.KeyboardAccelerators>
                </Button>
            </StackPanel>
        </Grid>

        <!-- 内容区域 -->
        <Grid Grid.Row="1" Padding="20">
            <!-- Grid View 全屏 -->
            <controls:ScheduleGridControl 
                x:Name="GridViewControl"
                GridData="{x:Bind GridData, Mode=OneWay}"
                Visibility="Collapsed"
                AutomationProperties.Name="排班表格全屏视图"
                AutomationProperties.LiveSetting="Polite"/>

            <!-- Position View 全屏 -->
            <controls:PositionScheduleControl 
                x:Name="PositionViewControl"
                ScheduleData="{x:Bind PositionScheduleData, Mode=OneWay}"
                Visibility="Collapsed"
                AutomationProperties.Name="哨位排班全屏视图"
                AutomationProperties.LiveSetting="Polite"/>
        </Grid>
    </Grid>
</Page>
