<ContentDialog
    x:Class="AutoScheduling3.Views.Scheduling.ShiftDetailsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="班次详情"
    CloseButtonText="关闭"
    DefaultButton="Close"
    Opened="ContentDialog_Opened"
    AutomationProperties.Name="班次详情对话框"
    AutomationProperties.HelpText="显示班次的详细信息，包括基本信息、人员信息和约束评估">
    
    <!-- 键盘快捷键 -->
    <ContentDialog.KeyboardAccelerators>
        <!-- Esc: 关闭对话框 -->
        <KeyboardAccelerator Key="Escape" Invoked="EscapeAccelerator_Invoked"/>
    </ContentDialog.KeyboardAccelerators>

    <ScrollViewer MaxHeight="600" MinWidth="500">
        <StackPanel Spacing="16">
            <!-- 基本信息 -->
            <StackPanel Spacing="8">
                <TextBlock Text="基本信息"
                           FontSize="16"
                           FontWeight="SemiBold"
                           AutomationProperties.Name="基本信息标题"
                           AutomationProperties.HeadingLevel="Level2"/>
                
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <!-- 日期时间 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="日期时间:"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       AutomationProperties.Name="日期时间标签"/>
                            <TextBlock x:Name="DateTimeText"
                                       Grid.Column="1"
                                       Text="2025-01-01 08:00-10:00"
                                       FontWeight="Medium"
                                       AutomationProperties.Name="班次日期时间"/>
                        </Grid>

                        <!-- 哨位 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="哨位:"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       AutomationProperties.Name="哨位标签"/>
                            <TextBlock x:Name="PositionText"
                                       Grid.Column="1"
                                       Text="哨位1 (主门岗)"
                                       FontWeight="Medium"
                                       AutomationProperties.Name="班次哨位"/>
                        </Grid>

                        <!-- 人员 -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="人员:"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       AutomationProperties.Name="人员标签"/>
                            <TextBlock x:Name="PersonnelText"
                                       Grid.Column="1"
                                       Text="张三"
                                       FontWeight="Medium"
                                       AutomationProperties.Name="班次人员"/>
                        </Grid>

                        <!-- 分配方式 -->
                        <Grid x:Name="AssignmentTypePanel"
                              Visibility="Collapsed">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="分配方式:"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Grid.Column="1"
                                        Orientation="Horizontal"
                                        Spacing="8">
                                <FontIcon Glyph="&#xE8FB;"
                                          FontSize="16"
                                          Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <TextBlock Text="手动指定"
                                           Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                           FontWeight="Medium"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 人员信息 -->
            <StackPanel Spacing="8">
                <TextBlock Text="人员信息"
                           FontSize="16"
                           FontWeight="SemiBold"
                           AutomationProperties.Name="人员信息标题"
                           AutomationProperties.HeadingLevel="Level2"/>
                
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel Spacing="12">
                        <!-- 技能 -->
                        <StackPanel Spacing="4">
                            <TextBlock Text="技能"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       FontSize="12"/>
                            <TextBlock x:Name="SkillsText"
                                       Text="基础哨位, 高级哨位, 夜间值守"
                                       TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- 当前工作量 -->
                        <StackPanel Spacing="4">
                            <TextBlock Text="当前工作量"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       FontSize="12"/>
                            <TextBlock x:Name="WorkloadText"
                                       Text="18 班次 (36小时)"/>
                        </StackPanel>

                        <!-- 本周工作量 -->
                        <StackPanel Spacing="4">
                            <TextBlock Text="本周工作量"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       FontSize="12"/>
                            <TextBlock x:Name="WeeklyWorkloadText"
                                       Text="5 班次 (10小时)"/>
                        </StackPanel>

                        <!-- 上一班次 -->
                        <StackPanel Spacing="4">
                            <TextBlock Text="上一班次"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       FontSize="12"/>
                            <TextBlock x:Name="PreviousShiftText"
                                       Text="2025-01-01 06:00-08:00 (哨位2)"/>
                        </StackPanel>

                        <!-- 下一班次 -->
                        <StackPanel Spacing="4">
                            <TextBlock Text="下一班次"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       FontSize="12"/>
                            <TextBlock x:Name="NextShiftText"
                                       Text="2025-01-01 14:00-16:00 (哨位3)"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 约束评估 -->
            <StackPanel Spacing="8">
                <TextBlock Text="约束评估"
                           FontSize="16"
                           FontWeight="SemiBold"
                           AutomationProperties.Name="约束评估标题"
                           AutomationProperties.HeadingLevel="Level2"/>
                
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16">
                    <StackPanel x:Name="ConstraintsPanel"
                                Spacing="8"
                                AutomationProperties.Name="约束评估列表"
                                AutomationProperties.LandmarkType="Custom">
                        <!-- 约束评估项将动态添加 -->
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- 操作按钮 -->
            <StackPanel Spacing="8">
                <TextBlock Text="操作"
                           FontSize="16"
                           FontWeight="SemiBold"
                           AutomationProperties.Name="操作标题"
                           AutomationProperties.HeadingLevel="Level2"/>
                
                <StackPanel Orientation="Horizontal"
                            Spacing="8">
                    <Button x:Name="EditButton"
                            Content="修改分配"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="EditButton_Click"
                            AutomationProperties.Name="修改分配按钮"
                            AutomationProperties.HelpText="打开修改分配对话框以更改此班次的人员"
                            ToolTipService.ToolTip="修改分配"/>
                    <Button x:Name="RemoveButton"
                            Content="取消分配"
                            Click="RemoveButton_Click"
                            AutomationProperties.Name="取消分配按钮"
                            AutomationProperties.HelpText="取消此班次的人员分配"
                            ToolTipService.ToolTip="取消分配"/>
                    <Button x:Name="ViewPersonnelButton"
                            Content="查看人员详情"
                            Click="ViewPersonnelButton_Click"
                            AutomationProperties.Name="查看人员详情按钮"
                            AutomationProperties.HelpText="查看此人员的完整信息"
                            ToolTipService.ToolTip="查看人员详情"/>
                </StackPanel>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
