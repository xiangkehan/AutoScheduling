<Page
 x:Class="AutoScheduling3.Views.History.HistoryPage"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:conv="using:AutoScheduling3.Converters"
 mc:Ignorable="d"
 x:Name="RootPage">

    <Page.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <conv:DateTimeFormatConverter x:Key="DateTimeFormatConverter"/>
    </Page.Resources>

    <Grid Padding="16" RowDefinitions="Auto,Auto,*">
        <!-- Filters -->
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBox Width="200" PlaceholderText="关键词" Text="{Binding Keyword, Mode=TwoWay}"/>
            <DatePicker Date="{Binding StartDate, Mode=TwoWay}"/>
            <DatePicker Date="{Binding EndDate, Mode=TwoWay}"/>
            <Button Content="应用" Command="{Binding ApplyDateRangeAsyncCommand}"/>
            <ComboBox Width="120" ItemsSource="{Binding SortByOptions}" SelectedItem="{Binding SelectedSortBy}"/>
            <Button Content="时间线" Command="{Binding SwitchViewCommand}" CommandParameter="Timeline"/>
            <Button Content="列表" Command="{Binding SwitchViewCommand}" CommandParameter="List"/>
        </StackPanel>

        <!-- Paging -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Visibility="{Binding IsListView, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="页码:"/>
            <TextBlock Text="{Binding CurrentPage}"/>
            <TextBlock Text="/"/>
            <TextBlock Text="{Binding TotalPages}"/>
            <Button Content="上一页" Command="{Binding PrevPageAsyncCommand}"/>
            <Button Content="下一页" Command="{Binding NextPageAsyncCommand}"/>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="2">
            <Grid.Resources>
                <DataTemplate x:Key="TimelineCardTemplate">
                    <Border Padding="12" Margin="0,8,0,0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="6">
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <StackPanel Width="100" VerticalAlignment="Center">
                                <TextBlock FontSize="18" FontWeight="Bold" Text="{Binding ConfirmTime, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd}"/>
                            </StackPanel>
                            <StackPanel Width="320">
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock>
 <Run Text="{Binding StartDate, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd}"/>
 <Run Text=" - "/>
 <Run Text="{Binding EndDate, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd}"/>
                                </TextBlock>
                                <TextBlock FontSize="12">
 <Run Text="人员:"/>
 <Run Text="{Binding NumberOfPersonnel}"/>
 <Run Text=" 哨位:"/>
 <Run Text="{Binding NumberOfPositions}"/>
                                </TextBlock>
                                <TextBlock FontSize="12">
 <Run Text="确认:"/>
 <Run Text="{Binding ConfirmTime, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd HH:mm}"/>
                                </TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Button Content="详情" Command="{Binding DataContext.ViewDetailCommand, ElementName=RootPage}" CommandParameter="{Binding Id}"/>
                                <Button Content="导出" Command="{Binding DataContext.ExportAsyncCommand, ElementName=RootPage}" CommandParameter="{Binding Id}"/>
                                <Button Content="对比" Command="{Binding DataContext.CompareCommand, ElementName=RootPage}" CommandParameter="{Binding Id}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </Grid.Resources>

            <!-- Timeline View -->
            <ScrollViewer Visibility="{Binding IsTimelineView, Converter={StaticResource BoolToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding GroupedHistorySchedules}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Key}" FontSize="20" FontWeight="Bold" Margin="0,16,0,4"/>
                                <ItemsControl ItemsSource="{Binding}" ItemTemplate="{StaticResource TimelineCardTemplate}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- List View -->
            <ListView x:Name="ListView" Visibility="{Binding IsListView, Converter={StaticResource BoolToVisibilityConverter}}" ItemsSource="{Binding Items}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="120,200,220,80,80,Auto" Padding="8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Margin="0,4,0,0" CornerRadius="4">
                            <TextBlock Grid.Column="0" Text="{Binding ConfirmTime, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}"/>
                            <TextBlock Grid.Column="2">
 <Run Text="{Binding StartDate, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd}"/>
 <Run Text=" - "/>
 <Run Text="{Binding EndDate, Converter={StaticResource DateTimeFormatConverter}, ConverterParameter=yyyy-MM-dd}"/>
                            </TextBlock>
                            <TextBlock Grid.Column="3" Text="{Binding NumberOfPersonnel}"/>
                            <TextBlock Grid.Column="4" Text="{Binding NumberOfPositions}"/>
                            <StackPanel Grid.Column="5" Orientation="Horizontal" Spacing="4">
                                <Button Content="详情" Command="{Binding DataContext.ViewDetailCommand, ElementName=RootPage}" CommandParameter="{Binding Id}"/>
                                <Button Content="导出" Command="{Binding DataContext.ExportAsyncCommand, ElementName=RootPage}" CommandParameter="{Binding Id}"/>
                                <Button Content="对比" Command="{Binding DataContext.CompareCommand, ElementName=RootPage}" CommandParameter="{Binding Id}"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>